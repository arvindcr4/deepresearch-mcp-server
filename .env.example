# ============================================
# REQUIRED ENVIRONMENT VARIABLES
# ============================================
# The application will not start without these variables set

# Neo4j Configuration (REQUIRED)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD= # REQUIRED - Set a strong password (avoid default 'password')

# ============================================
# OPTIONAL ENVIRONMENT VARIABLES
# ============================================
# These variables have sensible defaults if not provided

# Application Configuration
LOG_LEVEL=info # debug, info, warn, error (use 'info' or higher in production)
LOG_FORMAT=pretty # json, pretty (use 'json' for production, 'pretty' for development)
NODE_ENV=development # development, production, test

# Security Configuration
AUTH_REQUIRED=true # Set to false only for local development

# Database Backup Configuration
BACKUP_MAX_COUNT=15 # Maximum number of backup files to keep
BACKUP_FILE_DIR=/path/to/backup/directory # Full file path where backup files will be stored

# API Keys (Optional - Only required if using specific providers)
# SECURITY: Never commit actual API keys. Use this file as a template.
# Format requirements:
# - OpenAI: Must start with 'sk-' (e.g., sk-proj-xxxx...)
# - Perplexity: Must start with 'pplx-' (e.g., pplx-xxxx...)
# - XAI Grok: Must start with 'xai-' (e.g., xai-xxxx...)
OPENAI_API_KEY= # Required for OpenAI deep research tool
PERPLEXITY_API_KEY= # Required for Perplexity Sonar tool
XAI_GROK_API_KEY= # Required for xAI Grok tool
FIRECRAWL_API_KEY= # Required for Firecrawl integration
AGENTSPACE_API_KEY= # Required for Agentspace integration
AGENTSPACE_BASE_URL=https://api.agentspace.dev

# Rate Limiting Configuration (must be positive integers)
GLOBAL_RATE_LIMIT=30   # req/min - Overall rate limit for all tools
OPENAI_RATE_LIMIT=50   # req/min - OpenAI API rate limit
PERPLEXITY_RATE_LIMIT=30   # req/min - Perplexity API rate limit
XAI_GROK_RATE_LIMIT=40   # req/min - xAI Grok API rate limit

# Transaction Retry Configuration
TRANSACTION_MAX_RETRIES=3  # Maximum number of retry attempts for failed transactions
TRANSACTION_INITIAL_RETRY_DELAY_MS=100  # Initial delay between retries in milliseconds
TRANSACTION_MAX_RETRY_DELAY_MS=5000  # Maximum delay between retries in milliseconds
TRANSACTION_BACKOFF_MULTIPLIER=2  # Exponential backoff multiplier for retry delays
TRANSACTION_TIMEOUT_MS=30000  # Transaction timeout in milliseconds (30 seconds)
